apiVersion: v1
kind: Service
metadata:
  name: mini-terminal-backend-service
spec:
  selector:
    app: mini-terminal
    component: backend
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000